# -*- coding: utf-8 -*-


@rpc_method(deprecated=True, alternative_method="host_delete")
def deleteServer(self, serverId: str) -> None:  # pylint: disable=invalid-name
	self.host_delete(id=forceHostId(serverId))
